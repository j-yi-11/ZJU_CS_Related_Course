本程序实现图像缩放功能，函数输入为原始图像和缩放系数，输出为缩放后图像数据
使用方法：
①在文件夹内导入一张图片（后缀只能为.jpg,图像名只能为img）打开main.m,运行
②在命令行  请输入缩放系数:  后面输入缩放系数（系数＞1为放大，系数＜1为缩小）后按回车
③      Figure(1)  --  原来的图像
         Figure(2)  --  缩放后图像
注：
导入图片不能为二值图
